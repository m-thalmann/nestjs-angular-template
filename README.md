## Development

- Make sure to set a absolute `DATABASE_DATABASE` path in `apps/backend/.env` for the SQLite database file
- When updating the `.env` file, run `npx nx reset` to clear the cache before starting the backend again
